name = "schlima-api"
main = "src/index.ts"
compatibility_date = "2024-07-25"
compatibility_flags = ["nodejs_compat"]

# Secrets (set via `wrangler secret put`):
#   PASSWORD_HASH  — output of scripts/generate-hash.js
#   JWT_SECRET     — any long random string
